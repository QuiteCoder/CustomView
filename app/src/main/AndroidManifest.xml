<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hpf.customview">


    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomView">
        <activity android:name=".activity.MainActivity"
            android:theme="@style/Theme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ShutterButtonActivity"/>
        <activity android:name=".activity.ActivityScaleAnimation"/>
        <activity android:name=".activity.DashBoardViewActivity"/>
        <activity android:name=".activity.MaterialEditTextActivity"/>
        <activity android:name=".activity.MultilineTextActivity"/>
        <activity android:name=".activity.MultiTouchViewActivity"/>
        <activity android:name=".activity.ScaleProgressBarActivity"/>
        <activity android:name=".activity.ConstraintLayoutActivity"/>
    </application>

</manifest>